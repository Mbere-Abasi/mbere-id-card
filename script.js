// script.js
// Wait for the DOM to fully load window.addEventListener('DOMContentLoaded', () => { const card = document.querySelector('.id-card');
// When the card is clicked, toggle the flip class card.addEventListener('click', () => { card.classList.toggle('flipped'); }); });